/* ═══════════════════════════════════════════
   EVENT.JS — 이벤트 위임 레이어
   로딩 순서: db → state → api → ui → app → [event]

   현재 앱은 HTML onclick 속성을 직접 사용하므로
   이벤트 위임은 최소화되어 있습니다.
   온라인/오프라인, visibilitychange, SW 등의
   글로벌 이벤트는 app.js에 포함되어 있습니다.
═══════════════════════════════════════════ */
console.log('[EVENT] 이벤트 레이어 로드 완료');
